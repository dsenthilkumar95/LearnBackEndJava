CREATE TABLE userwithcompkey(
    FIRSTNAME VARCHAR(50),
    SECONDNAME VARCHAR(50),
    PASSWORD VARCHAR(50)
);
CREATE TABLE employee(
    ID INTEGER PRIMARY KEY,
    NAME VARCHAR(50),
    DOB DATE,
    employmentdate DATE,
    salary FLOAT,
    role VARCHAR(50)
);